import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.example.weathercodex',
  appName: 'Weather Codex',
  webDir: 'dist/weather-codex/browser'
};

export default config;
